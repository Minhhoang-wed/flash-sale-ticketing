spring:
  application:
    name: flash-sale-ticketing
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/flashsale}
    username: ${SPRING_DATASOURCE_USERNAME:flashsale}
    password: ${SPRING_DATASOURCE_PASSWORD:flashsale}
  data:
    redis:
      host: ${SPRING_DATA_REDIS_HOST:localhost}
      port: ${SPRING_DATA_REDIS_PORT:6379}
  rabbitmq:
    host: ${SPRING_RABBITMQ_HOST:localhost}
    port: ${SPRING_RABBITMQ_PORT:5672}
    username: guest
    password: guest
    listener:
      simple:
        # Ngày 7: retry in-memory 3 lần, delay 1s -> 2s -> 4s
        retry:
          enabled: true
          max-attempts: 3
          initial-interval: 1s
          multiplier: 2.0
          max-interval: 10s
        # Hết retry -> reject KHÔNG requeue -> broker đẩy sang DLX -> DLQ.
        # (true = requeue vô hạn: message độc quay lại đầu queue mãi mãi)
        default-requeue-rejected: false
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

app:
  # naive | pessimistic | optimistic | redis
  reserve-strategy: ${RESERVE_STRATEGY:redis}
  event-cache-ttl-seconds: 60
  # false = consumer không start -> message nằm chờ trong queue (demo Ngày 4)
  consumer-enabled: ${CONSUMER_ENABLED:true}
  # Hạn giữ chỗ (Ngày 5). Demo nhanh: RESERVATION_TTL_SECONDS=60
  reservation-ttl-seconds: ${RESERVATION_TTL_SECONDS:600}
  # Chu kỳ quét đơn quá hạn
  expire-scan-delay-ms: ${EXPIRE_SCAN_MS:30000}
  # Rate limit (Ngày 6): tối đa N request/giây/user vào /reserve
  rate-limit-per-second: ${RATE_LIMIT_PER_SECOND:5}

springdoc:
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    org.hibernate.SQL: debug
